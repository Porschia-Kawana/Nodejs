import React from "react";
import "./Home.scss";

function Home() {
    const [data, setData] = React.useState(null);

    return (
        <div className="Home">
            <p>home</p>
        </div>
    );
}

export default Home;
